# Superseded: Demo Client Implementation Plan

This document has been superseded by `docs/real-backend.md`.

The remaining work to make the demo client use a real backend is now consolidated in the real backend integration plan.

Use this file only as historical context if you need to understand the original UI build plan and sample-backed demo flow.
