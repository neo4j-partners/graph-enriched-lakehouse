# Deprecated local template.
# Prefer finance-genie/.env.sample copied to finance-genie/.env.
# This file remains for compatibility with older simple-finance-analyst-only workflows.

# Local app settings
USE_MOCK_BACKEND=true
DATABRICKS_APP_PORT=8000

# Required when USE_MOCK_BACKEND=false
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=replace-me

# Databricks SDK authentication for local runs
# Option A: use a named profile from ~/.databrickscfg.
DATABRICKS_CONFIG_PROFILE=<your-databricks-profile>

# Option B: use host/token directly.
DATABRICKS_HOST=https://<workspace>.azuredatabricks.net
DATABRICKS_TOKEN=dapi...

# Genie and SQL warehouse settings
GENIE_SPACE_ID=<space-id>
DATABRICKS_WAREHOUSE_ID=<warehouse-id>
SIMPLE_FINANCE_ANALYST_CATALOG=graph-on-databricks
SIMPLE_FINANCE_ANALYST_SCHEMA=graph-enriched-schema

# Used by app.yaml resource substitution during Databricks Apps deployment
WAREHOUSE_ID=<warehouse-id>
